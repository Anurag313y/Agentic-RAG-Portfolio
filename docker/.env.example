# Optional: use this file with `docker run --env-file docker/.env`
# Docker Compose loads `.dev.vars` from the repo root instead.
#
#   copy docker\.env.example docker\.env
#   copy .dev.vars.example .dev.vars

ADMIN_EMAIL=your-admin-email@example.com
ADMIN_PASSWORD=use-a-strong-password-here

# RESEND_API_KEY=
# RESEND_FROM=
# DEEPGRAM_API_KEY=
# COHERE_API_KEY=
# SMTP_USER=
# SMTP_PASS=
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
