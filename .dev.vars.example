# Copy this file to .dev.vars (gitignored) and fill in your values.
# Never commit .dev.vars to GitHub.
#
# Local:  cp .dev.vars.example .dev.vars
# Production (Cloudflare): wrangler secret put ADMIN_EMAIL
#                         wrangler secret put ADMIN_PASSWORD

ADMIN_EMAIL="your-admin-email@example.com"
ADMIN_PASSWORD="use-a-strong-password-here"

# Contact form email (SMTP)
# For security, SMTP settings (like your App Password) must be configured here via SMTP_PASS (locally) or as a secure secret in Cloudflare (in production).
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-16-char-app-password"
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="465"

# Optional fallbacks — Admin → API Configuration takes precedence when keys are saved there.
# DEEPGRAM_API_KEY="your-deepgram-api-key"
# COHERE_API_KEY="your-cohere-api-key"
